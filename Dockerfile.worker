# Dockerfile for worker (uses same image but explicit for clarity)
FROM python:3.11-slim
WORKDIR /code
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV PYTHONPATH=/code

CMD ["celery", "-A", "app.tasks", "worker", "--loglevel=info", "-Q", "builds"]
